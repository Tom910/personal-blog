---
import Nav from "@components/layout/Nav.astro";
import Tag from "@components/layout/content/Tag";
import TranslateList from "@components/translate/TranslateList";
import MainLayout from "@layouts/MainLayout.astro";
import { getAllTags, getLatestTranslatePosts } from "@utils/blog";
import SectionHeader from "@components/layout/content/SectionHeader";

const tags = await getAllTags();
const posts = await getLatestTranslatePosts();
---

<MainLayout title="Blog Posts">
  <Nav />
  <div id="blog" class="space-y-6">
    <div class="space-y-4 md:space-y-6 py-4">
      <SectionHeader title="Topics" description="Explore any topics" />
      <div class="flex flex-wrap items-center">
        {tags.map(tag => <Tag className="mb-4 mr-2" tag={tag} />)}
      </div>
    </div>
    <TranslateList posts={posts} />
  </div>
</MainLayout>
